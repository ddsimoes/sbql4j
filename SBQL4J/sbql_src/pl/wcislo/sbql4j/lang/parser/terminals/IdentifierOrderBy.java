package pl.wcislo.sbql4j.lang.parser.terminals;

public class IdentifierOrderBy  {
//	public enum SortType {
//		ASC,
//		DESC
//	}
	/*
	public final SortType sortType;
	
	public IdentifierOrderBy(String val, SortType sortType) {
		super(val);
		this.sortType = sortType;
		/*
		if(sortType == null) {
			throw new IllegalArgumentException("IdentifierOrderBy(): sortType=null");
		}
		SortType sortTypeTemp = null;
		for(SortType st : SortType.values()) {
			if(st.name().toLowerCase().equals(sortType)) {
				sortTypeTemp = st;
			}
		}
		if(sortTypeTemp == null) {
			throw new IllegalArgumentException("IdentifierOrderBy(): illegal sortType: "+sortType);
		} else {
			this.sortType = sortTypeTemp;
		}
		
	}
	
	public static void main(String[] args) {
//		System.out.println(SortType.valueOf("asc"));
		System.out.println(SortType.valueOf("ASC"));
	}
	*/
}
